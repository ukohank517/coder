#include <iostream>
#include <algorithm>
usint namespace std;

int N ;
int s[100];

int main(){
  int flag=0;//whether exist mod 10 not= 0
  int sum = 0;
  cin >> N;
  flag = 0;
  for(int i = 0;i < N ; i++){
    cin >> s[i];
    sum += s[i];
    if((s[i]%10)!= 0) flag =1;
  }
  if(flag == 0) {
    cout << "0" << end;
    return 0;
  }

  sort (s.begin(),s.end());
  
  
  return 0;
}
