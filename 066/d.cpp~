#include <iostream>
#include <vector>
#include <algorithm>
#define ll long long
#define N 100
#define MOD 1000000007
using namespace std;


vector <ll> fact(N+1);
vector <ll> rever_fact(N+1);


// in range 0 <= x <= N
// 1. x!
// 2. (x!)^{-1}
//   2-1. (N!)^{-1}
//   2-2. (N-1!)^{-1} = { (N!)^{-1} }* N
//   2-3. *****
void setfact(ll n){
  ll inp=1;
  ll a,b;
  
  //****************************1.x!************************
  fact[0]=1; //0!  
  for (ll i = 1;i <= n;i++){
    inp*=i;
    inp%=MOD;
    fact[i]=inp; 
  }

  //***********************2.(x!)^{-1}********************
  //***********2-1.(N!)^{-1}= (N!)^{MOD-2}****************
  rever_fact[n] = modulo(face[n],MOD-2);
  //***********2-2 
  for(ll i = n-1;i>=0;i--) rever_fact[i]=rever_fact[i+1]*(i+1);
  
}

ll modulo (ll a, ll b){
  ll x = 1 , y = a;
  while(b>0){
    if(b%2 == 1)x=(x*y)%MOD;
    y = (y*y)%MOD;
    b /= 2;
  }
  return x % n ;
}

//  aCb = n! / {r!*(n-r)!}
ll aCb(ll a, ll b){
  return fact[n]/(rever_fact[r]*rever_fact[n-r]);
}

int main(){
  setfact(N);
  for(int i =0;i<N+1;i++) cout << fact[i] << endl;
}
