#include <iostream>
#include <vector>
#include <algorithm>
#include <string.h>

#defint long long ll

ll dif(ll a , ll b){
  if(a>b) return a-b;
  else return b-a;
}

using namespace std;

ll n,a,b,c,d;

int main(){
  cin.tie(0);
  ios::sync_with_stdio(false);

  ll diff;
  ll a,b;
  
  cin >> n >> a >> b >> c >> d;

  if(a==b) cout << "NO" << endl;

  else{
    diff = dif(a,b);
    a = diff/c;
    b = diff/d;
  }
    
  return 0;
}
